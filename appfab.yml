# ~/.tmuxinator/appfab.yml
# you can make as many tabs as you wish...

name: appfab
root: ~/Sites/housetrip
pre_window: cd appfab
windows:
  - editor: vim
  - shell: 
  - database: rails db
  - server: rails s -p4445
  - console: rails c
  - dlog: tail -f log/development.log
  - tlog: tail -f log/test.log
  - shell: 
  - shell: 
