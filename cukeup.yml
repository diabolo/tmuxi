# ~/.tmuxinator/cukeup.yml
# you can make as many tabs as you wish...

name: cukeup
root: ~/Sites/
pre_window: cd cuke_up
windows:
  - editor: vim
  - shell: 
  - database: rails db
  - server: rails s
  - console: rails c
  - dlog: tail -f log/development.log
  - tlog: tail -f log/test.log
  - shell: 
  - shell: 
